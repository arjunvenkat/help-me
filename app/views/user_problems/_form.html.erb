<%= form_for(@user_problem) do |f| %>
  <% if @user_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_problem.errors.count, "error") %> prohibited this user_problem from being saved:</h2>

      <ul>
      <% @user_problem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :problem_id %><br>
    <%= f.number_field :problem_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
